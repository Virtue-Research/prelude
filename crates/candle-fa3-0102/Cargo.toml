# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# Pristine baseline of the OFFICIAL crates.io `candle-flash-attn-v3` v0.10.2
# (huggingface/candle). Renamed to `candle-fa3-0102` and EXCLUDED from the
# prelude workspace so it never gets confused with the heavily-modified fork at
# crates/candle-flash-attn-v3 (which is a 0.9.2 fork off rucnyz/candle with the
# hkernel_v3 / FlashAttnFwdSm90 / QPrologue changes). Depends on the official
# candle-core 0.10.2 from crates.io, NOT the workspace's rucnyz/candle fork.
[package]
edition = "2021"
name = "candle-fa3-0102"
version = "0.10.2"
build = "build.rs"
exclude = [
    "cutlass/docs/**",
    "cutlass/test/**",
    "cutlass/examples/**",
    "cutlass/tools/**",
    "cutlass/media/**",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Flash attention v3 layer for the candle ML framework."
readme = "README.md"
keywords = [
    "blas",
    "tensor",
    "machine-learning",
]
categories = ["science"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/huggingface/candle"

# Provenance — this crate IS the pristine official crates.io crate
#   candle-flash-attn-v3 = "0.10.2"
# (huggingface/candle @ git 7c7a8c57), renamed to candle-fa3-0102 for isolation.
# Verified 2026-06-09: src/ (lib.rs, ffi.rs) and all 84 hkernel/ files are
# byte-identical to upstream 0.10.2; build.rs differs ONLY by the static-lib
# rename (libflashattentionv3.a -> libcandle_fa3_0102.a). It does NOT depend on
# candle-flash-attn-v3 — it is that crate — so the upstream id is recorded here
# as metadata (cargo ignores [package.metadata]) instead of a circular dependency.
[package.metadata.upstream]
crate = "candle-flash-attn-v3"
version = "0.10.2"
git-sha1 = "7c7a8c570e6a16ea24cc30a7501c8ffbbdb51680"
source = "crates.io (huggingface/candle)"

[lib]
name = "candle_fa3_0102"
path = "src/lib.rs"

[[test]]
name = "flash_attn_tests"
path = "tests/flash_attn_tests.rs"

[[example]]
name = "bench_fa3_0102"
path = "examples/bench_fa3_0102.rs"

[dependencies.candle]
version = "0.10.2"
features = ["cuda"]
package = "candle-core"

[dependencies.half]
version = "2.3.1"
features = ["num-traits"]

[dev-dependencies.anyhow]
version = "1"
features = ["backtrace"]

[dev-dependencies.rstest]
version = "0.23"

# Re-added for tests only: upstream's Cargo.toml.orig had candle-nn as a `path`
# dev-dep, which crates.io strips on publish (path deps can't be published), so
# the bundled test (which uses candle_nn::ops::softmax for its reference impl)
# can't compile from the published crate. Pin to the matching official 0.10.2.
# Test-only — does NOT affect the library build or the FA3 kernels.
[dev-dependencies.candle-nn]
version = "0.10.2"
features = ["cuda"]

[build-dependencies.anyhow]
version = "1"
features = ["backtrace"]

[build-dependencies.cudaforge]
version = "0.1"

[build-dependencies.num_cpus]
version = "1.15.0"

[build-dependencies.rayon]
version = "1.7.0"
