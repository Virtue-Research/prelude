task = "classify"

concurrency = [1, 4, 8, 16, 32, 64, 128]
requests    = [50, 100, 100, 200, 200, 400, 400]
warmup      = 5
batch_size  = 20
seed        = 42

# output = "results/classify_realworld.json"

[data]
type    = "realworld"
dataset = "openai/gsm8k"     # needs: pip install datasets
