target/
target-codex-fa3/
.DS_Store
.idea
.vscode
/.env
temp/
.claude/
bench_results/
benchmark/__pycache__/
__pycache__/
/models/

# Local-only runs of external benchmarking harnesses
benchmark/results/
benchmark/e2e/results/
crates/onednn-ffi/build
onednn-ffi/buildCargo.lock
*.o
*.a
*.so
.cargo-ok
.cargo_vcs_info.json
*.orig
genai_bench.log

# Internal analysis/design notes — not checked in.
analysis/

# Per-developer agent instructions — not checked in.
CLAUDE.md

# Large benchmark dataset and generated sweep output — not checked in.
sampled_500x10.json
sampled_500_groups_x_10.jsonl
sweep_results/
_*.sh
_*.log
logs/

# Nsight Systems profiler output — large local-only artifacts (~600MB).
nsys_prof/
*.nsys-rep
*.sqlite

# ── Ad-hoc / superseded test scripts (kept locally, excluded from the repo) ──
# superseded by scripts/analyze_perreq_breakdown.py
analyze_fa3_breakdown.py
# superseded by bench_rps_clean.sh
bench_prelude.sh
bench_vllm.sh
# one-off verification / launch helpers
e2e_fuse_bench.sh
run_fuse_verify.sh
reproduce_gap_breakdown.py
microbench_vllm_attn.py
verify_fa.py
launch_prelude.sh
launch_vllm.sh
launch_vllm_docker.sh
# candle-fa3-0102 standalone-crate ad-hoc bench inputs
crates/candle-fa3-0102/bench_vllm_fa3.py
crates/candle-fa3-0102/bench_vllm_varlen_dataset.py
crates/candle-fa3-0102/gen_random_inputs.py
# candle-fa3-bench: legacy microbench for the fork FA3 path (superseded by
# prelude-cuda tests + verify tooling); removed from the workspace to stop
# --workspace builds from force-compiling the fork's CUTLASS kernels.
crates/candle-fa3-bench/

# ── Local scratch scripts and benchmark suite — not checked in. ──
scripts/
bench_scripts/
# machine-specific reproduction scripts (hardcoded paths + dataset refs)
bench_repro/
# one-off verification outputs
verify_out_*.txt

# ── Internal reports — not checked in. ──
reports/
